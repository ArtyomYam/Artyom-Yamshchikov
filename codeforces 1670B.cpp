#include <iostream>
#include <vector>

int main() {
	int t, n, k;
	std::vector<int>numbers(250);
	unsigned char ch = 0;
	std::cin >> t;
	for (int q = 0; q < t; q += 1) {
		std::cin >> n;
		numbers[n] = 1;

	}
}